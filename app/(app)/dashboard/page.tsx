/**
 * /dashboard — the workspace overview. Stub: the demo spine starts at a
 * single review, so there is no cross-review roll-up behind this screen.
 */

import StubScreen from "@/components/StubScreen";

export default function DashboardPage() {
  return (
    <StubScreen
      title="Dashboard"
      detail="Designed to hold the workspace at a glance — reviews still analyzing, findings waiting on a decision, and the trust score of every run that finished."
    />
  );
}
