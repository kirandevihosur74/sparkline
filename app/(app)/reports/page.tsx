/**
 * /reports — exported review summaries. Stub: the demo's one shareable
 * artifact is the signed audit trail, which has its own screen.
 */

import StubScreen from "@/components/StubScreen";

export default function ReportsPage() {
  return (
    <StubScreen
      title="Reports"
      detail="Designed to hold the exports a finished review produces — the findings, the decision on each one, and the signatures behind them, as a document that can be sent on."
    />
  );
}
