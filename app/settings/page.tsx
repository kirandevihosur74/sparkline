/**
 * /settings — workspace settings. Stub: the demo signs as a fixed reviewer,
 * so nothing on this screen is adjustable yet.
 */

import StubScreen from "@/components/StubScreen";

export default function SettingsPage() {
  return (
    <StubScreen
      title="Settings"
      detail="Designed to hold the workspace's reviewers, the signing identity attached to approvals, and the document and search provider credentials."
    />
  );
}
