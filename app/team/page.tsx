/**
 * /team — reviewers and signing identities. Stub: the demo signs every
 * decision as one fixed reviewer, so there is no roster to show.
 */

import StubScreen from "@/components/StubScreen";

export default function TeamPage() {
  return (
    <StubScreen
      title="Team"
      detail="Designed to hold the people who can decide findings, the signing identity each of them approves under, and which reviews are assigned to whom."
    />
  );
}
